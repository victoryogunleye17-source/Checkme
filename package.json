{
  "name": "checkme-real",
  "version": "1.0.0",
  "private": true,
  "description": "Real HTTP load testing tool that works on Vercel (short real tests)",
  "scripts": {
    "dev": "vercel dev",
    "start": "node server-local.js"
  },
  "dependencies": {
    "autocannon": "^7.15.0"
  },
  "engines": {
    "node": ">=18"
  }
}
